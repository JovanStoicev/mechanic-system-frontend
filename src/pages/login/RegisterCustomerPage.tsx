import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { useAuth } from "../../auth/AuthContext";
export default function RegisterCustomerPage(){ const {register}=useAuth(); const nav=useNavigate(); const [form,setForm]=useState({fullName:"",phone:"",email:"",address:"",password:""}); const [error,setError]=useState("");
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");try{await register(form);nav("/customer",{replace:true})}catch(x){setError(x instanceof Error?x.message:"Registration failed")}};
 return <div className="min-h-screen bg-slate-100 flex items-center justify-center p-4"><form onSubmit={submit} className="w-full max-w-md space-y-4 rounded-2xl border bg-white p-6 shadow"><h1 className="text-xl font-bold">Customer registration</h1>{error&&<div className="rounded bg-red-50 p-3 text-sm text-red-700">{error}</div>}{(["fullName","phone","email","address","password"] as const).map(k=><label key={k} className="block text-sm font-medium">{{fullName:"Full name",phone:"Phone",email:"Email",address:"Address",password:"Password"}[k]}<input className="mt-1 w-full rounded-lg border p-2" type={k==="password"?"password":k==="email"?"email":"text"} minLength={k==="password"?8:undefined} required={k!=="address"} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="w-full rounded-lg bg-slate-900 p-2 text-white">Create account</button><p className="text-center text-sm"><Link className="underline" to="/login">Back to login</Link></p></form></div>}
